server:
  port: 8762
spring:
  application:
    name: service-registry

eureka:
  instance:
    hostname: eureka-2
  client:
    service-url:
      defaultZone: http://eureka-1:8761/eureka/
    register-with-eureka: true
    fetch-registry: true