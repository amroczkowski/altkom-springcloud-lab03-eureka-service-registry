server:
  port: 8761
spring:
  application:
    name: service-registry

eureka:
  instance:
    hostname: eureka-1
  client:
    service-url:
      defaultZone: http://eureka-2:8762/eureka/
    register-with-eureka: true
    fetch-registry: true

